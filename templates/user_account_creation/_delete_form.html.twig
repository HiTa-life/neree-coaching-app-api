<form method="post" action="{{ path('user_account_creation_delete', {'id': user_account_creation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_account_creation.id) }}">
    <button class="btn">Delete</button>
</form>
